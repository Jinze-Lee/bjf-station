# ==========================================================================
# 仓库是公开的 —— 凡是不该被陌生人看到的，都必须在这里排除掉。
# 改这个文件前先想清楚：GitHub Pages 会把仓库里的**每一个文件**都对外提供。
# ==========================================================================

# --- 历史版本快照 ---------------------------------------------------------
# 本地保留（备份习惯不变），但不进仓库，原因有二：
#   ① git 本身就是版本管理，每次提交都是一个可回溯的快照，快照目录是重复劳动
#   ② Pages 会把它们一并对外提供 —— /_版本备份/v3.../index.html 是能打开的，
#      等于把几个半成品旧版站点也发布出去，还会被搜索引擎收录
_版本备份/

# --- 仪器原始导出 ---------------------------------------------------------
# 三个 dataTaker CSV + 一个径向生长 Excel。不进仓库，因为：
#   · 含未发表的完整监测记录，公开后等于先于论文放出原始数据
#   · 网页要用的是处理后的结果（js/observations.js），原始文件不参与页面运行
# 想公开时把下面这行注释掉即可，文件本身一直在本地。
原始数据/

# --- 处理后的 CSV 与全量明文 ---------------------------------------------
# 2026-08-03 补排除。此前这些**一直在公开提供** —— 实测
#   GET /数据/2026-07-10_2026-07-31_观测数据.csv  ->  200, 2,489,490 字节
# 完整原始表比 js/observations.js 还好用（下下来就是一张表，不用解析 JS）。
# 站点运行完全用不到它们：网页读的是 js/observations.js。
#
# 注意：这只挡住以后。已经推送过的版本仍留在 git 历史里，
# 要真正收回得把仓库设为私有，或者删库重建。
数据/

# 全量明文数据（发布脚本的输入）。公开的只有切出来的 7 天窗口，
# 完整数据集以 data/full.enc 的密文形式发布。见 工具/发布数据.py。
原始数据/全量/

# 文案批注稿：内部工作文件，不是给读者看的成品
docs/中文文案_待批注.md

# --- 中间产物 -------------------------------------------------------------
__pycache__/
*.pyc
.pytest_cache/

# --- 系统与编辑器垃圾 -----------------------------------------------------
Thumbs.db
desktop.ini
.DS_Store
*.swp
.vscode/
.idea/
~$*
