# 开发历程

本站从 2026-07-27 起搭建。2026-08-03 因需要收回早期已公开的完整数据，
仓库做过一次重建（历史里含未加密的全量观测数据与原始 CSV），
所以 git 历史从重建那次提交开始。

重建之前的 29 次提交完整保留在本地镜像备份中。下面是那段历史的逐条记录，
包含每次改动的原因 —— 这些决策理由比代码本身更难重建，所以整理成文档。

面向访客的版本说明见网站页脚的「更新日志」按钮，那份更简洁。

---

## 1. init: 北京森林生态系统定位研究站监测数据网站

`3ddb2c7` · 2026-07-28 18:11:26

> 单页站点，仿 treewatch.net 的风格与交互，中英双语（默认英文），
> 展示东灵山 20 棵树的液流通量密度与树干径向变化实测数据
> （2026-07-20 ~ 07-25，10291 行）。
>
> 主要构成
>   index.html          单页，7 个区块
>   js/chart.js         Highcharts Stock 双轴交互图表（核心）
>   js/map.js           Leaflet + 高德瓦片，含 WGS-84 到 GCJ-02 换算
>   js/network.js       数据传输示意图（液流与径向两条独立通路）
>   js/i18n.js          全站中英文词条
>   工具/               数据预处理、导入、校验、验收测试
>
> 为公开发布所做的准备
>   - Raleway 字体本地化到 css/fonts/，不再引 fonts.googleapis.com
>     （国内直连 Google 会卡到超时才回退系统字体）
>   - 加 .nojekyll，静态文件原样提供
>   - .gitignore 排除 _版本备份/ 与 原始数据/
>     仓库公开，原始仪器导出含未发表记录，不进仓库
>
> 验收：Playwright 驱动真实 Chrome，图表 35 + 页面 124 = 159 项全通过，
> 控制台零报错。

## 2. docs: 发布上线文档 + 一键更新脚本 + 验收脚本支持测线上

`6456d67` · 2026-07-28 18:20:43

> 站点已发布：https://jinze-lee.github.io/bjf-station/
> 对线上站跑验收 159 项全通过。
>
> 新增
>   docs/怎么发布上线.md   面向零基础的部署与运维说明
>   工具/发布更新.ps1      一条命令完成：列改动 -> 跑验收 -> 提交 -> 推送 -> 等构建
>                          验收不过就中止，不会把坏页面推上线
>
> 改动
>   验收脚本改为读 SITE_URL 环境变量，可对已发布的线上站跑同一套测试
>   （本地过不等于线上过：路径大小写、缺文件只有线上才暴露）
>   删除 docs/待填内容清单.md —— 内容已全部填完，留着会误导

## 3. fix: 发布脚本存为带BOM的UTF-8，Windows PowerShell 5.1 下才不会乱码解析失败

`8aa2e69` · 2026-07-28 18:24:38


## 4. feat: 换成暖白纸感配色 + 示意图加"可交互"提示

`c5e84e2` · 2026-07-28 19:54:14

> 配色
>   从 treewatch 那套深灰底(#202020)+饱和绿，改为米白底+深墨褐字+森绿标题+赤陶强调。
>   深色版留档 _版本备份/v9_深色版_改配色前_20260728/。
>
>   ★ 顺带把颜色收进变量：原先 178 处硬编码色值散在各处，换配色要逐个找。
>     现在全部走 :root 语义变量（209 处 var() 引用），再换风格只改一处。
>     js/chart.js 用 getComputedStyle 读同一套变量，图表自动跟随主题，
>     不会出现"页面换了配色、图表还是老颜色"的脱节。
>
>   地图样点色 --dt1 比标题绿亮一档(#4a7c59)：它要压在高德卫星影像上，
>   影像本身是绿褐调，深绿会直接糊进植被里看不见。同步改了生成树木数据.py。
>
>   对比度逐项量过，全部 WCAG AA，多数 AAA（正文 13.5:1，标题 7.2:1，次要文字 5.4:1）。
>
> 示意图"可交互"提示
>   问题：图里每个节点都能点开说明，但用户看不出来。
>   做法：半透明幽灵光标沿 11 秒周期在三个代表节点上各停一下、点一下，
>         配扩散涟漪；工具栏另加一句明说的提示。
>   收敛：用户真的动手（点/悬停/键盘聚焦）后动画永久停止 ——
>         提示只在没被理解时才有价值。
>
> 修掉两个 bug（验收里都加了断言防回归）
>   · 涟漪一直是不可见的：.gh-ring 加了 transform-box: fill-box，而 circle 的
>     fill 是 none，fill-box 因此是空盒，scale() 缩放空盒什么都没有。
>     动画在跑、computed style 一切正常，只有量 getBoundingClientRect 才发现宽高是 0。
>   · 图左上角有几个脏点：数据包用正的 begin 错开出发时间，到点前 SMIL 让元素
>     停在静态位置 (0,0)。改成负 begin，从第一帧起就在路径上。
>
> 验收
>   图表 35/35 + 页面 142/142 = 177 项全通过（页面新增第 9 组 18 项：
>   主题浅色、7 项文字对比度、幽灵光标行为、两个 bug 的回归断言、图表取色）。
>   控制台零报错。

## 5. fix: 发布脚本不再写死验收项数，改为从测试输出累加

`0164227` · 2026-07-28 19:57:05


## 6. feat: 改回深色主题为默认，并加日/夜切换按钮

`996bd74` · 2026-07-28 20:38:26

> 按要求回退到原来的深色配色，但保留这一轮新增的全部功能
> （幽灵光标、两个 bug 修复、CSS 变量架构、验收断言）。
> 暖白纸感没有丢：升级成可切换的第二套主题，表头右上角按钮切换。
>
> 主题系统
>   :root                      深色森林（默认，treewatch 源码实测配色）
>   :root[data-theme="light"]  暖白纸感
>   选择存 localStorage(bfers-theme)，默认深色且**不跟随系统** ——
>   深色是本站主视觉，不是"夜间模式"，不该被系统设置悄悄改掉。
>
>   顺带把剩下的主题相关值也收进变量：顶栏/页脚渐变、照片滤镜、
>   示意图 SVG 的中间色阶、放大镜、地图标记底。两套主题现在能完整表达。
>   新增 --accent-ink：纯橙在深色底上刺眼、浅色底上读不清，
>   "强调色文字"必须和"强调色"分开各给各的。
>
> 图表与地图跟随主题
>   光换 CSS 变量不够 —— Highcharts 把颜色烤进已生成的 SVG 属性，
>   Leaflet 标记颜色也是建 marker 时写死的。两者都注册 Theme.onChange：
>   图表销毁重建，地图逐个 setStyle/setIcon 并重建图层控件。
>   map.js 新增 plotColor()，从 --dt1/2/3 读色，取不到才回落 PLOTS[].color。
>
> 首屏不能闪屏
>   <head> 里加了一段同步内联脚本读 localStorage 设 data-theme。
>   只放在 js/theme.js 里就晚了：那个文件在 <body> 末尾加载，
>   届时已按默认深色画过一帧，偏好浅色的访客每次都会看到深色闪一下。
>
> 修掉本次引入的两个回归
>   · 加按钮后表头在 360/414px 横向溢出（scrollW 434）：
>     多出 42px 宽 + 14px 外距 + 一个 20px flex gap ≈ 76px。
>     根因是 .logo 设了 flex-shrink:0，空间不够时寸步不让。
>     放开它 + min-width:0 让站名成为唯一弹性项，比逐档硬调像素稳；
>     另加 480px 一档整体收紧。
>   · 图片加载断言把 Leaflet 瓦片和 loading="lazy" 的缩略图也算进去了，
>     等于把外部网络状况和浏览器的按需加载当成 bug。
>     改为只验本站图片，懒加载那批单独验"滚进视口后能加载"。
>
> 验收
>   图表 35/35 + 页面 164/164 = 199 项全通过（新增第 10 组 11 项：
>   切换、持久化、无闪屏、图表/地图跟随、按钮文案随语言、不破坏既有功能）。
>   对比度改为**两套主题分别逐项量**，全部 WCAG AA，多数 AAA。
>   控制台零报错。
>
> 留痕
>   暖白纸感完整快照 _版本备份/v10_暖白纸感_20260728/
>   深色原版快照     _版本备份/v9_深色版_改配色前_20260728/

## 7. data: 接入 7/10-7/28 实测数据（18.6 天，34412 点）+ 修正 Granier ΔT₀ 取法

`d6e4c66` · 2026-07-29 13:30:05

> 数据
>   液流   35 个数采导出文件（按 ZHL/LYS/QXZ 筛选、排除 TEST），成功读入 33 个
>   径向   径向生长 Excel 26 个树 sheet
>   窗口   2026-07-10 00:00 ~ 07-28 15:30，20/20 棵树
>   规模   10291 -> 34412 点，5.35 天 -> 18.6 天
>
>   已核对：新数据完整覆盖原有上线数据，无任何树木或时刻丢失
>   （旧 5154+5137=10291 点的每个时刻都能在新数据中找到）。
>   导入脚本会合并 数据/ 下所有批次，老批次 CSV 保留不动。
>
> ★ 修正 ΔT₀ 取法：全期单一最大值 -> 7 天滑动窗口最大值
>
>   记录从 5 天扩到 33 天后，昼夜振幅比全面塌掉（中位 9.3 -> 2.9），
>   夜间基线从接近 0 抬到二三十。查下来是 Granier 的经典陷阱：
>
>     K = (ΔT₀ - ΔT) / ΔT，ΔT₀ 在分子里
>     -> ΔT₀ 被任何一个异常高值抬高，**每一个点**的 Fd 都系统性偏高
>
>   证据
>     · ΔT₀ 比逐日最大值中位数高得越多的树，昼夜比掉得越狠，一一对应
>       DT2-HTQ2-1157 +44.9% -> 比值 36.9 掉到 2.9
>       DT2-MD2-1249  +41.4% -> 18.3 掉到 2.5
>     · DT2 的 LYS1/LYS2/WJF1 三棵树，全期最大 ΔT 落在 07-15 22:30 同一时刻
>       —— 三棵树同秒达到最大温差是设备事件，不是生理现象
>     · 多数 ΔT₀ 出现在 06-26~07-09，即展示窗口之外
>       —— 窗口外的一个坏点污染了窗口内的每一个点
>
>   改用滑动窗口后：中位比值回到 5.2，DT2-HTQ1-1248 从 3.1 回到 11.9。
>   DT0_METHOD 可选 moving/daily/global，理由写在脚本顶部。
>
> 预处理脚本增强
>   · 按规则批量筛文件（8位日期+ZHL/LYS/QXZ，排除含 TEST，大小写不敏感）
>   · 取交集改为**按来源类型**聚合，不再按文件 —— 数采每次导出都是"从头到现在"
>     的全量，35 个文件范围互相嵌套，按文件取 max(起点) 会被最新的小文件
>     掐死，窗口塌成几小时
>   · 认 12 小时制时间戳，救回被 Excel 另存过的 20260717LYS-2.csv
>   · 只剩时刻没有日期的（如 '3:00:00 PM'）明确跳过并报错，不猜日期
>     —— 跳过 20260717QXZ.csv / 20260720LYS.csv，其覆盖已由同期其他文件提供
>
> 验收 199 -> 200
>   0-data-e 拆成两条：起点必须完全一致（能抓住裁剪逻辑写错）；
>   终点允许参差但差异不超过 15% —— QXZ 最后导出到 07-27 16:00，
>   ZHL/LYS 到 07-28 15:30，6 棵 DT3 液流早结束 1 天是设备现实不是 bug。
>   图表 36/36 + 页面 164/164 全通过，控制台零报错。

## 8. ui: 精简数据信息条 + 站点信息改两列 + 方法学文案口语化

`25830cd` · 2026-07-31 00:35:23

> 按要求改的三处
>   · 数据信息条去掉「导入时间」和「数据批次」，合并为「最新更新」。
>     批次是给维护者看的内部信息（CSV 文件名+行数），对访客无意义还撑长框子；
>     需要追溯时 数据/*_meta.json 里一直有完整记录。
>   · 站点信息表改双列（<=760px 退回单列）。条目本身都很短，
>     一行一条时右边大片留白，两列后同样内容占一半高度。
>   · 「为何报通量密度而非整树液流」重写。原文像规格书，
>     改成先说人话再讲道理，并补上「为什么不用异速生长方程去估边材面积」——
>     那才是这个选择真正的理由。
>
> 体检发现并修掉的
>   · methods.sampling 里「取交集后裁剪，使全部序列覆盖同一区间」已不成立：
>     三台数采各自导出，QXZ 比 ZHL/LYS 早结束一天，6 棵 DT3 的液流并非同一区间。
>     改成如实说明，并声明图上留空不插补。
>   · 同段补入 ΔT₀ 取法的说明。上一版把方法从"全期最大值"改成"7 天滑动窗口"，
>     但页面上没提 —— 方法变了而公开描述没跟上，等于文档失真。
>
> 关于「防止别人扒数据」
>   试过把坐标从 7 位小数降到 5 位，**实测否掉并回滚**：
>     · DT1-LDL1-1155 与 DT1-WJF1-1135 实际相距 1.86 m，5 位算成 1.11 m，
>       误差 0.75 m。地图专门缩到 z20 就是为了分开相邻的树，降精度会破坏它。
>     · 而且这根本不构成保护 —— 能走到样地的人，坐标 1 cm 还是 1.1 m 都找得到仪器。
>   结论写进脚本注释，免得以后有人再想一遍。
>   数值精度本来就只有 2 位小数（0.11 / 0.15），已经是"够画图、不够直接拿去分析"。
>
> 验收 201 -> 201（4y2 拆成 4y2 + 4y3，后者反向断言不再暴露批次信息）
>   图表 36/36 + 页面 165/165，控制台零报错。

## 9. fix: 更正液流回传方式（蒲公英路由器组网，非云平台）+ 排版收行长

`e093286` · 2026-07-31 12:19:37

> 事实更正 —— 这条是错的，必须改
>   原文写「数采经以太网接入 AgriPhenoCloud 平台回传」。实际是：
>   数采本身不联网，接到现场的蒲公英路由器上，由贝锐蒲公英做异地组网，
>   把野外数采并入虚拟局域网，实验室侧像访问本地设备一样连上去取数。
>   四处同步改掉（方法学正文、数据传输段落、示意图节点名、图例），中英文都改。
>     net.modem      无线传输单元 -> 蒲公英路由器
>     net.modemSub   打包上行     -> 异地组网回传
>     net.legendMix  数采打包后上行 -> 经路由器回传
>
> 排版：正文行长
>   实测容器 1140px、字号 15px 时一行装 147 个字符 —— 排版舒适区是 45–75，
>   超了整整一倍。读到行尾往回找下一行起点容易串行，不是观感问题是可量化的。
>
>   给「要逐字读的」加行长上限（段落/脚注/公式/信息条/示意图说明），
>   「宽度本身是信息量的」不加（表格/图表/地图/示意图/图集）。
>
>   数值是**实测调出来的不是推的**：
>     · 46em 看着像 46 字，实测 92 字（拉丁小写平均宽约 0.5em）
>     · 68ch 实测 86 字（ch 是数字 0 的宽度，比小写字母宽）
>     · 最终 55ch = 505px，实测平均 66 字、最长 77、无一行超 78
>   校准办法写进注释了：Range 逐字符取 rect.top，top 一变即换行。
>
>   台站照片从左浮动改成右浮动并放大到 430px。正文收窄后右边空出约 500px，
>   照片留在左边会有两个毛病：绕排段落每行只剩 41 字符（比别处窄一半），
>   以及照片结束后文字突然满宽、上下行起点差 330px。挪到右边后两栏稳定，
>   段落宽度自始至终一致；放大到 430px 是为了填掉否则会出现的 300px 空带。
>
>   公式块也收到正文同宽 —— 原先一个短公式摆在 1140px 的框里，两侧大片空白。
>
> 验收 201 -> 203
>   新增 9n/9o 两条守住行长：平均须落在 45–78，且无一行超 82。
>   逐行实测而非按 px 估算 —— 估算这次已经证明会错一倍。
>   图表 36/36 + 页面 167/167，控制台零报错。

## 10. revert: 撤销排版改动；图集换入两张机箱内部照

`c960c3a` · 2026-07-31 12:48:17

> 回退
>   上一版收窄正文行长(--measure 55ch)、台站照片改右浮动 —— 实际效果不好看，
>   按要求整体还原 css/style.css 到 v12。对应的两条行长断言(9n/9o)一并移除。
>   同批的**事实更正保留**（蒲公英路由器组网，非云平台）—— 那是纠错不是排版。
>
> 图集换图
>   install-3 / install-4 原先是"树干上的探头"，与 1、2 号内容高度重复。
>   换成两张机箱内部照，正好补上"数采和联网设备长什么样"，
>   与 Methods 里讲的异地组网互相印证：
>     3 号  紫色 DT80 + 可直接插 SIM 卡的蒲公英路由器（四根天线）
>     4 号  蓝色 DT80 Series 4 无插卡功能，箱门加装 4G 工业路由器；
>           另有 MPPT 太阳能充电控制器，该样点靠太阳能供电
>   图注按实际拍到的设备重写，中英文都有。
>   原 4 号图注里"隔热层下部有意不封死、留通风缝隙防树皮腐烂"是方法学要点，
>   已并入 2 号图注（那张正好拍到隔热层），没有丢。
>
> 顺带修掉两个新图带来的问题
>   · 主图框原先固定 430px 高，是按四张全竖构图定的。换入横构图后上下留宽黑边、
>     机箱设备看不清。试过 4:3（横图满了但竖图被压到 297px 高），
>     最终用 1:1 —— 竖图 297x396、横图 396x297，长边都拿到 396px，两边都不吃亏。
>   · 窄屏横向溢出（360px 实测 scrollW 412）：窄屏规则里列宽写的是 1fr，
>     而 1fr 的下限是内容尺寸，主图加 aspect-ratio 后倒推出 400px 宽度需求，
>     把这一列撑爆。改成 minmax(0, 1fr) 才真正跟着容器走。
>
> 验收 36/36 + 165/165 = 201 项全通过，控制台零报错。

## 11. fix: 图集第 2 张换成仰拍通风缝隙那张，四张不再有重复

`509d25e` · 2026-07-31 16:00:29

> 上一次只换掉 3、4 号，1、2 号仍然都是"树干上的传感器"，还是重复。
> 现在把 2 号也换掉，改用从下往上拍隔热层下缘的那张（原 4 号图，
> 留档在 _版本备份/v12.../install-4_旧.jpg），四张各讲一件事：
>
>   1  树上的整体安装：径向变化仪 + 液流探头同株
>   2  仰拍隔热层下缘：下部有意不封死留出的通风缝隙
>   3  机箱内部：可直接插卡的蒲公英路由器
>   4  机箱内部：无插卡功能的数采 + 加装 4G 工业路由器
>
> 顺带修掉一个之前没发现的问题
>   英文 gal.cap2 的两次改写其实都**没生效** —— 用 PowerShell 的 .Replace()
>   按整句匹配，而文件里的实际措辞与我以为的不同（"another tree"/"probes"/
>   "wrapping" vs "another stem"/"probe"/"insulation"），不匹配时它静默跳过、
>   不报错。截图核对时才发现图换了、英文图注还是旧的。
>   已改用精确匹配修正，并补了一段校验：按关键词逐条核对八条图注（中英各四）
>   是否与所配图片对得上，避免同类静默失败再发生。
>
> 验收 36/36 + 165/165 = 201 项全通过。
>
>
>  images/install-2-thumb.jpg | Bin 42132 -> 33725 bytes
>  images/install-2.jpg       | Bin 227367 -> 144285 bytes

## 12. data: 接入 7/28-7/31 新增数据（21.98 天，41044 点）

`e1c671c` · 2026-08-01 18:56:49

> 时间范围  07-10 00:00 ~ 07-31 23:30   （原 07-10 ~ 07-28 15:30）
>   规模      34412 -> 41044 点，18.65 -> 21.98 天，20/20 棵树
>   已核对    旧数据 34412 点的每个时刻都能在新数据里找到，一棵树、一段时间都没少
>
> ★ 修掉一个会静默丢数据的 bug：多个径向生长 Excel 被整键覆盖
>
>   dendro.update(got) 是 dict 的整键替换 —— 两个 Excel 都含 1131 这棵树时，
>   后读的会把先读的**整条序列顶掉**，而不是接起来，日志上还照常显示"读取成功"。
>
>   这次正好踩上：径向生长文件夹里 4 个 Excel 各是一段滚动窗口，谁也不含谁
>       (2)112   07-10 ~ 07-28   <- 上次网站用的
>       (1)      07-20 ~ 07-25
>       (1)11    07-26 ~ 07-29
>       (2)11    07-27 ~ 07-31   <- 本次新增
>   按文件名排序后 (2)11 最后读，用 update 的话 07-10~07-26 那段会整段消失，
>   而这恰恰是网站上已有的数据。
>
>   改成按树号 extend 再统一 snap_series 去重；取交集时也把所有 Excel 当作
>   **一个**来源（与液流侧的处理一致），否则会被覆盖最窄的那个文件掐死。
>
> 数据质量
>   昼夜振幅比 7.3 -> 8.0（记录变长后 7 天滑动窗口的 ΔT₀ 更稳）
>   全部序列起止完全一致，终点参差 0%（上批 QXZ 早结束一天，为 5.3%）
>
> 验收 36/36 + 165/165 = 201 项全通过，控制台零报错。

## 13. feat: 新增环境条件面板（11 个变量，彩色标签开关，与树木图表共用时间轴）

`3aa8c1c` · 2026-08-02 17:34:37

> 数据来源换了
>   改用台站气象站导出的 data.xlsx，弃用径向生长 Excel 里网关 1148 那套温湿度。
>   查证结果：1148 的湿度是坏的 —— 气温-湿度相关系数只有 -0.29（真实 RH 应
>   -0.7~-0.95），34% 的读数恰好等于 99.9，多天振幅为 0；07-12、07-23、07-24
>   整天纹丝不动，是传感器进水/结露漂到量程顶的形态。1130 的 h 更糟（56% 贴顶）。
>   新文件的湿度 r=-0.68、多数天振幅 20~56%，且与降雨对得上（07-10~12 连续降雨
>   39.8mm 那几天贴顶，雨停后振幅立刻回到 56%），物理逻辑自洽。
>
> 新增 js/environment.js（11 变量 11616 点）
>   光照 空气温度 相对湿度 降雨量 土壤水分 土壤温度 气压 风速 风向 PM2.5 PM10
>   不收电压V（设备状态）与径向生长mm（站点单点，而本站已有 20 棵树的逐树高精度记录）
>   时间窗裁到与 observations.js 一致（07-10 00:00 ~ 07-31 23:30），两图才能共用时间轴
>
>   两个坑写进注释了：
>   · 各 sheet 列顺序不同（202607 第2列是空气温度，202608 第2列是 PM2.5）-> 必须按列名取
>   · OBS_META 是 json.dumps(indent=2) 写出的，键带引号且数组跨行，
>     第一版正则 span:\s*\[(\d+),\s*(\d+)\] 匹配不上却不报错，安静跳过裁剪
>
> 面板设计
>   不做双轴 —— 11 个变量量纲从 klux 到 hPa 到 µg/m³，两个轴根本不够分。
>   改成彩色标签开关：点一下加一条曲线，颜色与标签色点一致。
>   默认只开光照 + 空气温度（驱动蒸腾的直接因子，与上方液流对应关系最直观）。
>   选 1-2 个时显示彩色纵轴可直接读数；选 3 个以上时纵轴挤不下，隐藏并明确写出
>   「各曲线独立缩放，只能比形状与时相，不能比数值」—— 这个取舍有代价，说在明处。
>
>   降雨用柱状（事件量，折线会在两场雨之间连出并不存在的斜坡）
>   风向用散点（0° 与 360° 同向，折线会在越过正北时拉出贯穿全图的竖线）
>
> 共用时间轴
>   两图互相监听 afterSetExtremes 并调用对方 setExtremes，实测双向同步误差 0 ms。
>   必须有 syncing 标志：A 通知 B，B 的 setExtremes 又会触发它自己的事件再通知回 A，
>   不拦就是无限循环、页面直接卡死。验收 11g 专门守这条。
>
> 验收 201 -> 215（新增第 11 组 14 项）
>   图表 36/36 + 页面 179/179，控制台零报错。
>
> 留档：_版本备份/v14_双图表前_已上线满意版_20260802/（含 git 提交号与回退说明）

## 14. feat: 两张图上下紧贴 + 底部共用一条时间轴

`8ba184e` · 2026-08-02 18:04:23

> 原先两张图各挂一条 navigator，中间那条把上下切断，而且不确定该拖哪一条。
> 现在整页只有一条时间轴，在最底下，同时控制两张图。
>
>   上图（液流+径向）  rangeSelector 按钮在顶部；navigator / scrollbar / 日期刻度全部关掉
>   下图（环境条件）    navigator + scrollbar + 日期刻度在底部，拖它两张图一起走
>
> 绘图区左右边距写死成同一个值（PLOT_MARGIN = 92，chart.js 与 chart-env.js 各一份）。
> 不固定的话两张图各按自己的轴标签宽度算边距（气压四位数、光照两位数），
> 绘图区会差出几十像素——同一时刻不在同一条竖线上，叠放就失去意义。实测 92+916 完全一致。
>
> 修掉三处会破坏"贴在一起"的东西
>   · 上图的日期刻度：两张图各写一遍就成了两条时间轴，中间那排数字把上下切断
>   · 上图的图例：原先在底部，正好卡在两图中间 -> 挪到图上方，点击开关序列的功能保留
>   · 标签栏外层 flex-wrap：11 个标签占两行后把「全选/清空」挤到第三行，
>     白白多出一整行 -> 外层改 nowrap，只让标签内部换行。间距 113px -> 71px
>
> ★ 首屏两张图时间范围对不上（这个不改就直接穿帮）
>   主图的 rangeSelector 默认选「1周」，建图时就把自己缩到最后 7 天；
>   环境图是后建的、显示全区间。而联动只在"有人操作"时触发，首屏没人操作过 ——
>   于是上下是两段完全不同的时间（实测上图 7/25-7/31、下图 7/10-7/30）。
>   改为绑定事件后立刻把主图当前区间同步过来。验收 11r 专门守这条。
>
> 环境变量的文字说明移到图表下方（原先夹在两图之间）。
>
> 验收测试也要跟着改：navigator 挪到下方图后离页顶更远，常常不在视口里，
> 而 page.mouse 用视口坐标——不滚进来就是对着空白拖，现象是"不报错但区间纹丝不动"。
> 4c / 5c 加 scroll_into_view_if_needed()。
>
> 验收 215 -> 219（新增 11o~11r：时间轴唯一性、不重复画刻度、绘图区对齐、首屏同步）
>   图表 36/36 + 页面 183/183，控制台零报错。
>
> 留档：_版本备份/v15_环境面板_独立时间轴_20260802/（含 git 提交号与回退说明）

## 15. feat: 标签栏移到时间轴下方 + 跨图表联动指针 + 风向改箭头与风玫瑰图

`75c5b5e` · 2026-08-02 18:46:20

> 一、标签栏挪到时间轴下面
>   原先夹在两张图中间，拖 navigator 手柄时 Highcharts 会在手柄上方弹出日期气泡，
>   正好被标签挡住 —— 看不见自己拖到了哪一天。
>
> 二、跨图表联动指针
>   鼠标停在任一张图上，两张图同时出现竖线，并弹出**一个**提示框，
>   里面同时列出该时刻的液流、径向变化，以及当前选中的全部环境变量：
>
>       Tuesday, 28 Jul, 03:30
>       ● Sap flux density: 46.7 g m-2 s-1
>       ● Radial change: -230.9 um
>       ──────────
>       ● Light: 6.6 klux
>       ● Air temperature: 22.6 C
>
>   竖线用 crosshair + snap:false，直接跟鼠标像素走。两张图的绘图区左右边距是
>   写死对齐的（PLOT_MARGIN=92），所以同一个 chartX 就是同一时刻，像素值可以直接
>   递给另一张图。提示框只在鼠标所在那张图上弹（两个框会互相遮挡），但内容合并。
>   查值用二分而不是线性扫：每次鼠标移动都要调用，序列两千多点，线性扫会发涩。
>
> 三、风向：散点 -> 箭头 + 风玫瑰图（用户选 C）
>   原来的散点不好，原因不止是难看：
>   · 角度当数值画，0° 与 360° 同向，越过正北会拉出贯穿全图的竖线（折线时）；
>     散点没这条线，但读者仍要把「210 这个数」在脑子里翻译成「西南风」
>   · 更要紧的是**大部分时候根本没风**：中位风速仅 0.43 m/s，57% 的时刻 <0.5，
>     16% <0.2（气象上的静风）。风速接近 0 时风向读数基本是噪声，
>     而散点把噪声和真实风向画得一样醒目
>
>   现在：
>   · 箭头贴在环境图底部，每 6 小时一个（30 分钟采样、1056 点全画的话每个不到 1px），
>     长度与不透明度随风速变化；风速 <0.2 m/s 不画
>   · 箭头指「风吹去的方向」（气象风向角是来向，故 +180°）
>   · 右侧风玫瑰图，16 方位，随当前时间窗重算；只在勾选风向/风速时出现
>   · 风向不再占用纵轴刻度（它不画线，那条 0-360° 的轴留着只会让人以为有线没显示）
>
>   主导风向用**矢量平均**：359° 与 1° 的算术平均是 180°（正南），与实际正好相反。
>   实测全区间 N 354°，22 天里 19 天主导风向落在 N/NNW/NNE，是山谷地形的典型信号。
>
> 验收 219 -> 228（新增 11s~11A）
>   含一条容易踩的坑：验收里动鼠标前必须先把图表滚进视口 ——
>   page.mouse 用视口坐标，图表在页面下方时坐标落在视口之外，
>   事件根本到不了 Highcharts，现象是"不报错但提示框和竖线都不出现"。
>   我自己就先踩了一次，误以为是实现有问题。
>
>   图表 36/36 + 页面 192/192，控制台零报错。

## 16. fix: 删除风玫瑰图；上图补回日期刻度；箭头疏密随缩放自适应

`9a6f1e7` · 2026-08-02 19:06:20

> 一、删除风玫瑰图
>   HTML 容器、CSS、i18n 词条、JS 里的 drawRose 与 ROSE_DIRS，以及对应的
>   4 条验收断言，全部清干净。
>
> 二、上方（液流+径向）图表补回日期刻度
>   之前为了"整页只留一条时间轴"把它关掉了，实际用起来看上图时要低头到下图
>   去找日期，很别扭。两张图各标一次更好用 —— 反正刻度位置完全一致
>   （绘图区左右边距是写死对齐的，实测两张都是 92+916）。
>   底部的 navigator + scrollbar 仍然只有一条，不变。
>
> 三、★ 修掉"风速和风向更新频率不一样"的错觉
>   用户反馈风向看着比风速更新慢。查数据：**两者完全一致** ——
>   时间戳逐一相同，都是 1056 个点、30 分钟一次，风速 98.1%、风向 98.6%
>   的相邻点都在变化，最长连续不变都只有 2 个点（1 小时）。
>
>   是我画法造成的：风速画满 1056 个点，箭头却写死每 12 个点（6 小时）画一个，
>   并排看就像风向采样稀疏。
>
>   改成按当前视窗宽度倒推步长，目标相邻箭头至少隔 18px：
>     全区间(22天)   1056 个采样点 ->  51 个箭头 ( 5%)
>     约 5.5 天       264 个采样点 ->  44 个箭头 (17%)
>     约 1.3 天        64 个采样点 ->  32 个箭头 (50%)
>   放大后接近逐点，与风速曲线疏密一致；缩小时自动抽稀，不糊成一团。
>   验收 11y 守这条。
>
> 验收 228 -> 226（删掉玫瑰图的 4 条，新增箭头自适应 1 条，改写日期刻度那条）
>   图表 36/36 + 页面 190/190，控制台零报错。

## 17. ui: 时间轴移到两图中间；中文正文重写，去掉翻译腔

`a2115e5` · 2026-08-02 19:31:04

> 一、时间轴挪到两个面板中间
>   navigator + scrollbar 从下方环境图搬回上方图表底部，正好落在两图之间。
>   下方图表不再重复挂，整页仍然只有一条时间轴。
>   环境图高度从 408 收回 340（时间轴搬走了），绘图区左右仍严格对齐（92+916）。
>   顺手清掉 chart-env.js 里已无人调用的 navData()。
>
> 二、中文正文重写
>   用户反馈"大段中文 AI 味太浓"。先量了一下，确实有迹可循：
>     破折号 ——        17 处   很多是"话说一半再补一句"
>     <strong> 强调    29 处   满屏加粗等于没有重点
>     最长一段         748 字   methods.sampling
>   改后：破折号 9 处、加粗 14 处，最长的一段拆成三节。
>
>   重写了 12 段（about.p1/p4、trees.p1/p2、methods.why/calib/dendro/sampling、
>   net.p、env.p/note/scaleNote）。方向不是套模板，是通读后按这几条改：
>     · 长短句错开。原来每句都四平八稳、字数相近，读着像列清单
>     · 去掉"需要说明的是""值得一提的是"这类空转的连接词
>     · 该说人话的地方说人话，数字、方法、限制照旧严谨
>   英文未动。
>
> 验收修了两条被时间轴挪位置带偏的断言
>   · 0m 原来用 .highcharts-xaxis-labels 这种页面级选择器取主轴刻度。
>     navigator 自己也是一条 xAxis、DOM 上带同样类名，挪回主图后就把它的
>     %m/%d 刻度混了进来（实测取到 '07/13' 而不是 '07月13日'）。
>     改用 Highcharts API 明确取 xAxis[0]，xAxis[1] 才是 navigator。
>   · 11o 断言时间轴在下图，现在改为断言在上图底部；"只有一条"这条不变。
>
>   图表 36/36 + 页面 190/190 = 226 项全通过，控制台零报错。

## 18. revert: 恢复原中文文案；修提示框在深色主题下发白

`2dcad48` · 2026-08-02 19:46:44

> 一、中文文案回退
>   上一次重写的 12 段全部恢复到改动前（js/i18n.js 回到 9a6f1e7）。
>   破折号 17 处、<strong> 29 处、methods.sampling 748 字，与重写前逐项一致。
>   时间轴挪到两图中间那部分改动**保留**，只回退文案。
>
>   例外一处：env.note 里"风向以散点呈现"是**事实错误** —— 风向早已改成箭头。
>   回退会把这个错误一起带回来，所以单独把这一句改正（中英同步），
>   并补上"箭头指风吹去的方向、静风不画"。除此之外没动任何文风。
>
> 二、修提示框在深色主题下发白
>   chart.js 里 tooltip.backgroundColor 写死了 'rgba(255,255,255,0.97)'，
>   是浅色主题时期留下的。深色主题下上方图表的提示框就成了一块刺眼的白，
>   而下方环境图的是深的 —— 同一页面两种底色。
>   改成跟着 --surface 走。实测：
>     深色  两张图都是 #262626
>     浅色  两张图都是 #ffffff
>
> 验收 36/36 + 190/190 = 226 项全通过，控制台零报错。

## 19. copy: 按逐条意见修改中文文案 + 观测方法改用小节标题

`77c8507` · 2026-08-02 20:21:40

> 结构调整
>   「液流通量密度。」「树干径向变化。」「采样与数据处理。」这三句原先是写在
>   段首的粗体短语，本身不成句、读着像病句。提成真正的小节标题（<h4 class="m-sub">），
>   段落随之从完整句子起头。英文同步，段首那三个粗体短语一并去掉。
>
> 逐条修改（只动中文）
>   trees.p2        删掉「因此这样的搭配是有意为之」
>   methods.caveat  「需要说明的是」-> 「在此需要注意」
>   methods.calib   「本页的 Fd」-> 「本试验的 Fd」
>   methods.why     开头改为「本图展示的是通量密度，而不是整树的精确耗水量」；
>                   「还得乘上」-> 「还需要乘上」，并把「钻取树芯」提到前面：
>                   「还需要钻取树芯、得到这棵树的边材横截面积」；
>                   删掉「那等于拿别处树的规律往这里硬安」，改为
>                   「异速生长方程虽然也能估，但精确度不高」
>   methods.sampling 删掉「ΔT₀ 的取法值得单独说一句。」
>
> ★ 有一处没有照原话改，说明一下
>   用户要把「最直觉的做法是取整段记录里的最大温差」改成「最佳做法」。
>   但这句的重点恰恰是**这个做法有问题**（会被断电/加热异常抬高，把整条序列推高），
>   说成「最佳做法」会与后半句自相矛盾。
>   改为「ΔT₀ 最简单的取法是整段记录里的最大温差」—— 同样直白，意思不反。
>
> 同时更新 docs/中文文案_待批注.md，方便继续提意见。
>
> 验收 36/36 + 190/190 = 226 项全通过，控制台零报错。

## 20. copy: 注明风速/风向传感器装在林下

`f5b455a` · 2026-08-02 20:38:38

> 图注补一句：传感器装在林下，读数普遍偏小（全期中位数 0.43 m/s），
> 反映的是林内近地面风况，不能代表林冠之上。中英同步。
>
> 这一句有必要写：0.43 m/s 这个量级，读者若默认是标准气象观测高度（10 m）
> 会以为设备有问题；注明林下之后，这个数值就是合理的，而且明确了适用范围。
>
> 验收 36/36 + 190/190 = 226 项全通过。

## 21. feat: 联系方式末尾加站点版本行 v1.5.2

`4c0f78b` · 2026-08-02 20:58:55

> 网站版本 v1.5.2 · 数据更新至 2026-07-31
>   Site version v1.5.2 · data updated to 2026-07-31
>
> 位置在 contact 区块最后、二维码之下，一条细分隔线隔开。样式做得很轻
> （12px、--text-faint）—— 它是给回访者确认「数据有没有更新」用的，不该抢正文。
>
> 日期不写死，由 js/data-source.js 从 OBS_META.span 自动取。
> 手写的日期迟早会忘了改，到时候页面写着旧日期、图表里是新数据，比不写还糟。
> 改版本号只需改 i18n 里 site.version 一处。
>
> 版本号依据：上线后累计 5 个新功能（日夜主题、幽灵光标、环境面板、
> 共用时间轴+联动指针、风向箭头）故次版本为 5；其后两次文案修订故修订号 2。
>
> 顺带修一处：插入 i18n 词条时先做了一次全局替换，把中文那两条也写进了
> 英文词典（英文侧出现重复键，后者覆盖前者，表面看不出来）。已清理，
> 现在中英各一条，逐条核对无误。
>
> 验收 36/36 + 190/190 = 226 项全通过。

## 22. ui: 去掉「实测数据」信息条

`9997f4f` · 2026-08-02 21:14:43

> 实测数据下这块面板整个隐藏。时间范围图表自己就写着，最新更新时间已挪到
> 联系方式末尾的版本行，这块属于重复。
>
> ★ 但警示路径保留了
>   合成数据（source='sample'）和数据文件缺失时，橙色警示条仍会出现。
>   那是防止「合成数据被当成实测发布」的最后一道闸，不能因为现在用不上就一并删。
>   实测分支会把 display 设成 none，所以两条警示分支都要显式改回 '' ——
>   否则一旦走过实测分支，之后即使换成合成数据，警示条也再不会出现
>   （语言切换会重新调用 render，这个坑很容易踩）。
>   已实测：临时把 source 改成 'sample'，警示条正常恢复显示。
>
> 验收：4y 改为断言"实测下不显示"，4y2/4y3（原本检查信息条内容）
> 合并为一条"数据日期改由版本行呈现"。
> 图表 36/36 + 页面 189/189 = 225 项全通过，控制台零报错。

## 23. feat: 页脚改为树种组成条，数字改为从 trees.js 现算

`e90c9ec` · 2026-08-02 21:49:41

> 原来的「20 棵监测树木 / 分布于 3 个样点」两个数字方块和参照站点完全一样，
> 且 20 和 3 是写死在 HTML 里的，加树加样点会悄悄过期。
>
> 改成一条堆叠组成条：外层按木材解剖构造分四个色块，色块内部用缝隙切出树种，
> 下接图例与汇总行。棵数、树种数、样点数、各段比例全部从 TREES 现算。
>
> 不直接按 8 个树种上色，是因为实测分布是 8 个树种里 5 个各只有 2 棵，
> 并排就是五段几乎等宽的碎块，页脚宽度连标签都放不下；按木材构造归并后是
> 55/20/15/10，而这也正是页面自己写明的「控制液流日变化曲线形态的最主要因素」。
>
> 顺带修两处：
> - --footer-bg 是渐变，拿它当 border-color 会被当无效值丢掉，分隔缝画不出来。
>   新增纯色变量 --fc-gap（双主题各一份）。
> - 环孔材/半环孔材原配色明度太近，橙色两段糊在一起，拉开为金/锈。
>
> 验收 234/234 通过（页面 198 + 图表 36），控制台零报错。
> 1440/1024/768/414/360 五档、深浅双主题、中英双语均已截图确认。

## 24. perf: 图片压缩 66% + 地图/图表改按需加载，首屏传输减少约 1.9 MB

`50b4bdc` · 2026-08-02 23:06:13

> 实测国内访问 4.1 秒加载完，逐项量过之后做两件事。
>
> 一、图片（省 1377 KB，66%）
> 新增 工具/压缩图片.py：按实际显示尺寸重采样 + 转 WebP，原图移入 images/_原图/。
> 最大的浪费是 station.jpg —— 文件 1400x1050，页面实测只显示 300x225（4.7 倍），
> 489 KB 压到 91 KB。四个 logo 都是 4~6 倍过大，合计 276 KB 压到 17 KB。
> net-bg.jpg 在 CSS 里本来就带 blur(2.5px)，砍到 1000px 宽肉眼无差，380→128 KB。
> 顺带清掉无人引用的 sensor-installation.jpg（173 KB）。
>
> 不做 <picture> 回退：本站已在用 CSS 自定义属性、flex gap、Highcharts，
> 在不支持 WebP 的浏览器上早就全线崩，加回退没有意义。
>
> 二、按需加载（首屏少 560 KB）
> 新增 js/lazy-load.js：IntersectionObserver 盯 #location 与 #treedata，
> 进入视野前 600px 才串行加载对应脚本。原先这些全在首屏抢带宽：
>   地图  leaflet.js 42 + leaflet.css 4 + 高德瓦片 157 KB
>   图表  highstock.js 132 + observations.js 178 + environment.js 50 KB
>
> 配套改动：
> - OBS_META 拆出成 js/obs-meta.js（1.2 KB）。首屏只有 data-source.js 需要它，
>   就为了页脚那行「数据更新至 X」，不该为此下 178 KB 的观测数据。
>   两份文件由 _数据文件格式.py 的同一个 meta 字典生成，不会不一致。
> - CDN 回退从 document.write 改成检测式。页面加载完后再 document.write
>   会清空整个文档。
>
> 验收 237/201+36 通过，新增按需加载断言 3 条（含「首屏确实没下」这条）。

## 25. ui: 页脚收成一行「20 棵树 · 8 个树种 · 3 个样点」

`81f0fce` · 2026-08-02 23:28:17

> 上一版是堆叠组成条 + 木材构造图例 + 汇总行，共三行。信息是全的，
> 但页脚不该让人停下来解读 —— 要配图例才看得懂，就说明它在这个位置
> 讲不清自己。木材构造那层信息在「监测树木」一节的表格里本来就有。
>
> 现在一行：数字用主题绿加粗立住，单位弱化成灰。360px 下占 183px，
> 放得下不折行。信息比原先那两个数字方块还多一项（树种数）。
> 数字仍然全部从 trees.js 现算。
>
> 顺带清掉只为组成条加的 --fc-gap 变量。
> 分隔点原先用 --footer-border（近黑），在深色页脚上等于没画，改用弱色压透明度。
>
> 验收断言从 10 条收到 4 条，其中「数字必须等于 TREES 现算值」保留。

## 26. feat: 更新日志弹层 + 版本号升到 v1.6

`3d47746` · 2026-08-02 23:39:51

> 版本行右边加「更新日志」按钮，点开弹出从第一版到现在的完整变更记录，
> 共 13 条：v0.1 起步 -> v1.0 首次上线 -> v1.6 性能优化。
>
> 内容与日期取自 _版本备份/ 的 15 个存档时间和 git 提交记录，不是事后追记。
> 中英各写一份 —— 这个站是双语的，只写中文等于对英文访客关掉这个功能。
>
> 实现要点：
> - 弹层 DOM 首次点击时才建，不占首屏。
> - 打开时锁 body 滚动，否则滚轮穿透到正文；Esc 与点遮罩都能关。
> - 变更类型标签用边框不用填充：页面其它地方没有实心色块的语言。
>
> 新增断言 12 条，其中 6o 守「版本行的版本号必须等于日志最新一条」。
> 顺手修 4y2 —— 它把 "v1.5" 写死在断言里，这次升版就红了，改成正则匹配。

## 27. fix: 下拉框把 20 棵树全标成「本批无数据」并禁用

`b6c612d` · 2026-08-03 12:06:48

> 按需加载改动的回归。tree-info.js 的 hasData() 读 OBS_FD，而 OBS_FD 在
> 按需加载的 observations.js 里 —— 下拉框在首屏就建好，那时数据还没下，
> 于是每棵树都被判成无数据、全部 disabled，整个下拉框不能用。
>
> 改法：数据未到时一律按「有数据」处理（信息不足就不要下判断），
> observations.js 到位后由 lazy:chart 事件触发重建，再把标注补上。
>
> 顺带修一个更早就在的 bug：hasData() 查的是 OBS_DIA，但数据文件里
> 根本没这个全局，叫 OBS_RAD。所以「只装了径向仪、没装液流」的树一直会被
> 误判成无数据。当前 20 棵树两套仪器都齐（实测 OBS_FD 20 / OBS_RAD 20），
> 所以看不出来，但下一批数据就会踩。
>
> 新增断言 3 条：数据未到时不得有禁用项；数据到位后标注必须与 OBS_FD/OBS_RAD
> 的实情逐棵一致（有数据却被禁、无数据却可选，两个方向都查）。

## 28. feat: 完整数据集加密，公开层只留最近 7 天

`0af2a15` · 2026-08-03 12:48:25

> 站点是纯静态托管，凡是发布的文件谁都能按 URL 下载，所以「藏起来」没用
> （密钥写在 JS 里，扒下来就解了）。这里的做法是：**公开的那份本来就只有
> 7 天**，完整那份是密文。
>
> 新增 工具/发布数据.py
>   原始数据/全量/*.js（全量明文，不进仓库）
>       ├─► js/observations.js   最近 7 天，295 KB，13,401 点（全量的 33%）
>       ├─► js/environment.js    最近 7 天
>       ├─► js/obs-meta.js       元信息（公开窗口 + 完整数据集规模，供提示条用）
>       └─► data/full.enc        全量，gzip 后 AES-256-GCM，218 KB
>
>   先压缩再加密，顺序不能反 —— 密文不可压缩，反过来传输量会从 178 KB
>   涨回 904 KB，把之前的性能优化全吃掉。
>   口令经 PBKDF2-SHA256 60 万轮派生，随机盐 16 字节，随机 IV 12 字节。
>   口令不写进任何文件。
>
> 新增 js/unlock.js：图表下方的解锁条。口令错误由 GCM 认证标签直接拒绝，
> 不需要也不存在「部分解开」的中间状态。解锁后换掉全局并重建两张图。
>
> 密文实测：香农熵 7.9992/8.0，256 种字节值全部出现，再 gzip 反而涨
> 0.04%（无残留结构），搜不到任何明文特征串。
>
> 同时堵掉一个一直在漏的口子
>   数据/*.csv 此前一直被 Pages 公开提供 —— 实测 GET 返回 200、2.4 MB，
>   是完整原始表，比 js/observations.js 还好用。已移出仓库并加进 .gitignore。
>
> 改掉一个会自动复发的隐患
>   两个导入脚本原先都写 js/，下次导入新数据就会把 7 天公开版覆盖成全量明文，
>   随下一次 push 泄出去。改成写 原始数据/全量/，结束时提示要跑发布脚本。
>
> 新增断言 15 条：公开窗口不得超过声明天数（观测与环境各一条）、错误口令
> 必须被拒且不改动任何数据、正确口令后跨度与点数须等于元信息声明的完整规模、
> 刷新保持解锁、清会话回到锁定。
>
> ⚠️ 已推送过的旧版本仍在 git 历史里。要真正收回，需把仓库设为私有或删库重建。

## 29. docs: 更新日志补 v1.7 条目，站点版本升到 v1.7

`8b6748c` · 2026-08-03 12:53:22

